# PlaneFlow — Лига сезона (сезонные соревнования)

> **Статус: Фаза 1 (MVP на mock) — В КОДЕ (2026-07-01 два прохода; хвосты закрыты 2026-07-02).**
> Овнер подтвердил отход от «без сезонов» именно для этой реализации (см. ниже — сам инвариант
> «медаль=навсегда» не нарушен). Сделано: `season`-бакет в `periodBucket`, 5 клиентских дивизионов
> по перцентилю против сид-ботов **(боты масштабируются под уровень игрока)**, **promotion/relegation
> между сезонами**, кумулятивные пороговые бейджи дивизиона (`comp:true`, мимо «Легенды»), вкладка
> «Сезон» на экране рейтинга (countdown + дивизион + промо-индикатор + приз), один косметический
> приз на сезон в отдельном сторе, дивизион в шеринг-карточке, **тост-приглашение «Стартовал
> Сезон N» активным игрокам**. Код: [`07-audio-services.ts`](../../../src/game/07-audio-services.ts)
> (`season*` функции, `seasonBotAnchor`, `Leaderboard.season` + `invite`/`ackInvite`),
> [`12-achievements-medals.ts`](../../../src/game/12-achievements-medals.ts) (`ACH.onSeasonDivision`),
> [`11-menu-ui.ts`](../../../src/game/11-menu-ui.ts) (вкладка),
> [`10-scene-loop.ts`](../../../src/game/10-scene-loop.ts) (`drawShareCard`),
> [`08-gameplay.ts`](../../../src/game/08-gameplay.ts) (тост в `reset()`). Тесты —
> [`tests/leaderboard.test.mjs`](../../../tests/leaderboard.test.mjs) + авто-e2e в
> [`tests/e2e.spec.mjs`](../../../tests/e2e.spec.mjs). Не сделано: чип-**отсчёт** на старт-экране
> (портретный баг самого экрана уже починен PR #391 — просто ещё не реализован),
> мягкая валюта (овнер отклонил) и Фазы 2-3 (Android/бэкенд) — см. «Осталось» ниже.
>
> Документ описывает перенос в PlaneFlow механики **Google Play «Leagues»** (кейс Subway
> Surfers, [play.google.com/console/about/subway-surfers-leagues-casestudy](https://play.google.com/console/about/subway-surfers-leagues-casestudy/))
> — **ограниченное по времени соревнование на уже-считаемых метриках + дивизионы + награды.**
>
> Строится поверх уже готового соревновательного каркаса — см.
> [`leaderboards.md`](leaderboards.md) (каркас `Leaderboard`/`Account`, `periodBucket`,
> ранг-медали) и боевой бэкенд [`backend-firebase.md`](backend-firebase.md).
> Обзорно — в [`../../backlog.md`](../../backlog.md).

---

## ⚠️ Это НОВОЕ продуктовое решение (отход от «без сезонов») — ПОДТВЕРЖДЕНО 2026-07-01

Дизайн-док рейтингов фиксирует прежний выбор овнера:
[«ранг-медали — пороговые **НАВСЕГДА**, без сезонов/перевыдачи»](leaderboards.md#ранг-медали--пороговые-навсегда).
**Сезонная лига этот выбор нарушает** — по определению это повторяющееся окно с перевыдачей.
Как и сами рейтинги когда-то были сознательным отходом от «лёгкой меты», лига — это
осознанный шаг, который овнер должен подтвердить **до** реализации (см. «Открытые вопросы»).

Чтобы конфликт был минимальным, план **разводит два класса наград** (см. ниже): постоянные
пороговые медали остаются как есть, а «сезонным» становится только косметика + дивизион-бейджи,
которые тоже пороговые-навсегда. То есть модель «навсегда» мы не ломаем — добавляем поверх неё
ротацию контента.

---

## Зачем (урок кейса)

Главный вывод кейса — **не награды, а цифры**: у Subway Surfers Leagues **+45 %
игрового времени** у записавшихся, **75 % записавшихся — уже вовлечённые игроки**,
**+3 % инсталлов** от самого ивента, и — ключевое для нас — успех достигнут
*«минимальными усилиями разработки, переиспользовав уже отслеживаемые метрики»*
(у них — монеты и время; у нас — **обслуженные борта / combo / streak / earned**).

Единственный настоящий рычаг ретеншена здесь — **повторяющийся повод вернуться**:
«до конца сезона 3 дня, ты 2-й в Серебряном дивизионе». Всё остальное — обвязка вокруг него.

---

## Что переиспользуем (почти нулевая цена)

| Нужно для лиги | Уже есть в коде |
| --- | --- |
| Приём/ранжирование счёта | `Leaderboard.submitRun` / `top` / `bestScore` — [`07-audio-services.ts`](../../../src/game/07-audio-services.ts) |
| Периодизация (UTC, детерминированная) | `periodBucket(ts)` — all/month/week, там же |
| Свопаемый провайдер (mock → сервер) | `Leaderboard.provider` (тот же приём, что `Analytics.sink`) |
| Метрика соревнования | Survival: `served` (обслуженные борта); также `combo`, `noCrashStreak`, `earned` в [`06-state-layout.ts`](../../../src/game/06-state-layout.ts) |
| Экран рейтинга + переключатель периодов | `showLeaderboard` / `renderLeaderboard` / `lbPeriod` — [`11-menu-ui.ts`](../../../src/game/11-menu-ui.ts) |
| Пороговые медали + мимо `RUN_CAP` | `ACH` / `ACH.onRank`, флаг `comp:true` — [`12-achievements-medals.ts`](../../../src/game/12-achievements-medals.ts) |
| Нативные топы (Android) | Play Games `PG_LB_SURVIVAL`, `showLeaderboard` — [`12b-native-play-games.ts`](../../../src/game/12b-native-play-games.ts) |
| ~~Сезонная косметика как приз~~ | ~~скины воркбенча (`assets/skins/`)~~ — **отпало:** система скинов игры убрана целиком (`CLAUDE.md`, один скин `neon`); `assets/skins/` — это тюнинг-инструмент превью, не игровые ассеты. Приз реализован как акцентный цвет, без зависимости от скинов. |
| Чипы на старт-экране (отсчёт/приглашение) | `updateStartChips` — [`11-menu-ui.ts`](../../../src/game/11-menu-ui.ts) (⏳ не подключено — старт-экран сейчас визуально ломается в портрете, см. `../../backlog.md`) |
| Тесты соревновательного слоя | [`tests/leaderboard.test.mjs`](../../../tests/leaderboard.test.mjs) |

---

## Дизайн

### 1. Сезон = новый бакет `periodBucket`

Сейчас `periodBucket` отдаёт `all` / `YYYY-MM` / `YYYY-Www`. Добавляем **`season`**:
детерминированное 2-недельное окно UTC от фиксированного якоря.

```
SEASON_EPOCH = <фикс. понедельник 00:00 UTC>   // константа в конфиге
SEASON_LEN   = 14 * 24 * 3600 * 1000
seasonIndex(ts) = Math.floor((ts - SEASON_EPOCH) / SEASON_LEN)   // 0,1,2,…
ключ бакета     = 'S' + seasonIndex             // напр. 'S37'
номер для показа = seasonIndex + 1              // «Сезон 38»
```

Свойства сохраняем те же, что у существующих периодов: чистая функция, UTC, одинаковая
на клиенте и будущем сервере, срез = фильтр журнала по текущему бакету (без разрушающего
сброса — старые счета живут в all-time). Длину/якорь — в конфиг, чтобы менять без правки логики.

✅ **Сделано как описано** — `SEASON_EPOCH = Date.UTC(2026,0,5)` (понедельник),
`SEASON_LEN = 14 дней`, `seasonKey/seasonNumber/seasonDaysLeft` в
[`07-audio-services.ts`](../../../src/game/07-audio-services.ts); `periodBucket()` отдаёт
дополнительное поле `season`. ⚠️ **Одно отличие от плана:** `season` НЕ добавлен в массив
`PERIODS` (тот питает `ranks`/`onRank` — топ-100/топ-10/№1 «навсегда»), чтобы место в крошечном
сезонном бракете не подделывало глобальные ранг-медали; UI-вкладки берут отдельный список
`LB_TABS = PERIODS.concat(['season'])` в [`11-menu-ui.ts`](../../../src/game/11-menu-ui.ts).

### 2. Дивизионы (Bronze → Diamond)

Вместо одного глобального топа (где ты 4000-й) — **бракеты по силе**: мотивирует быть 2-м из 30.

- 5 дивизионов: 🥉 Bronze · 🥈 Silver · 🥇 Gold · 💠 Platinum · 💎 Diamond.
- **Назначение:** первый сезон игрока — по перцентилю его all-time `bestScore`; далее —
  **promotion/relegation** по итогу прошлого сезона (топ ~20 % ↑, низ ~20 % ↓).
- ⚠️ **Настоящие кросс-игроковые дивизионы без сервера невозможны** (та же причина, что
  глобальный топ — см. [`leaderboards.md`](leaderboards.md#статус-это-каркас-не-боевой-рейтинг)).
  На mock-провайдере дивизион — это честная **демонстрация** против сид-ботов
  ([`07-audio-services.ts` → `seedBots`](../../../src/game/07-audio-services.ts)): бракет и
  ботов подбираем под уровень игрока (✅ сделано, см. ниже). Боевые дивизионы приходят с
  бэкендом (Фаза 3).

✅ **Сделано:** `seasonDivisionIndex(rank, total)` — перцентиль в сезонном топе (квинтили
0.2/0.4/0.6/0.8) для *первого* сезона игрока; `Leaderboard.season.standing(mode)` отдаёт
`{rank, total, divisionIdx, division, list, promoted, relegated}`. **Promotion/relegation
сделаны (вторым проходом, 2026-07-01):** `pf_season_division_v1` хранит последний
назначенный дивизион с его `seasonKey`; `resolveSeasonDivision(rec, curSeasonIdx, rank, total)` —
чистая функция: рекорд ЭТОГО сезона → дивизион зафиксирован (не пересчитывается повторными
заходами); рекорд ПРОШЛОГО сезона → `seasonPromote()` поднимает/опускает на 1 по тем же
квинтилям 0.2/0.8; пропущенные сезоны или первый заход → свежее назначение по перцентилю.
UI показывает «▲ Повышение! / ▼ Понижение» на вкладке «Сезон».

✅ **Подбор ботов под уровень игрока — сделано (2026-07-02).** `seedBots` больше не фиксированный
список и **не персистится** (боты — деталь mock-провайдера, вычисляются на чтении). Они
масштабируются под «уровень игрока» = его лучший survival-счёт из **прошлых** сезонов
(`seasonBotAnchor` в [`07-audio-services.ts`](../../../src/game/07-audio-services.ts)): при
референсном уровне (~`BOT_ANCHOR`=24) раскладка совпадает с прежними фикс-скорами, выше/ниже —
линейно, чтобы игрок попадал в осмысленный бракет (не всегда последний и не всегда первый).
Текущий сезон из анкора **исключён** намеренно: иначе боты гнались бы за только что поставленным
рекордом и прогресс внутри сезона был бы невозможен; раз в сезон бракет пере-анкорится на новый
уровень — как промо/релегейт в настоящих лигах. Проверено в браузере: свежий игрок со счётом 12
попадает 7-м из 9 → дивизион Серебро (мид-бракет, а не «всегда последний»). ⏳ **Осталось:**
настоящие бракеты по дивизиону (не общий список) — ждёт бэкенда (Фаза 3).

### 3. Награды — два класса (мирим с «навсегда»)

| Класс | Что | Модель | Легенда? |
| --- | --- | --- | --- |
| **Постоянные бейджи дивизиона** | «Достиг Gold», «Достиг Diamond» и т.п. | пороговые **навсегда** (как ранг-медали) — флаг `comp:true` | **нет** (мимо «Легенды», как `comp:true`) |
| **Сезонная косметика** | 1 акцентный цвет за сезон (или за место) | **ротация**, хранится в отдельном реестре наград, не в `unlocked`-Set | н/д |
| (опц.) Мягкая валюта | бонус `money` за место | разовое начисление | н/д (не сделано) |

Ключевой приём: **`ACH.unlocked`-Set не трогаем ротацией** — туда попадают только
пороговые-навсегда бейджи дивизиона. Ротирующиеся призы идут в **отдельный
стор наград** (`pf_season_rewards_v1` по аналогии с `pf_account_v1`), чтобы не ломать
инвариант «медаль = навсегда» и подсчёт «Легенды».

✅ **Сделано, с ОДНИМ отходом от плана:** бейджи `season_bronze`…`season_diamond` (`comp:true`,
кумулятивные — достиг Diamond, получил и все четыре ниже) выдаёт `ACH.onSeasonDivision()`;
косметика — `Leaderboard.season.claimReward(divisionIdx)`/`.reward()` в `pf_season_rewards_v1`,
как и спланировано. ⚠️ **Отход:** план предполагал подавать косметику «через существующую
систему скинов» — эта система **убрана целиком** между написанием плана и реализацией (см.
`CLAUDE.md`: один скин `neon`, `SKIN_DEFS` и выбор скина не существуют больше). Вместо неё —
акцентный цвет (кружок-индикатор рядом с дивизионом на вкладке «Сезон»), самодостаточный, без
зависимости от снятой системы. Мягкая валюта — не сделана (не было в MVP-скоупе).

### 4. UX — отсчёт и «запись»

- **Отсчёт** «До конца сезона: N дн.» — ✅ на экране рейтинга (вкладка «Сезон», `#lbSeason` в
  [`11-menu-ui.ts`](../../../src/game/11-menu-ui.ts)). ⏳ **НЕ сделан** чип на старт-экране
  (`updateStartChips`) — портретная раскладка старт-экрана, которая раньше блокировала эту
  задачу, уже починена (PR #391, см. [`../../backlog.md`](../../backlog.md)); чип просто пока
  не добавлен.
- **Приглашение в лигу — сначала своим активным** (у SYBO 75 % записавшихся уже играли): ✅
  **сделано (2026-07-02).** Логика таргетинга — `Leaderboard.season.invite({minBest})` /
  `.ackInvite()` в [`07-audio-services.ts`](../../../src/game/07-audio-services.ts): чистая
  синхронная функция над локальным стором (`pf_season_invite_v1`) — «активный» = есть личный
  рекорд (`bestScore ≥ minBest`, т.е. закончил хотя бы один заход), `show` = раз за сезон (до
  `ackInvite`). Поверхность — **тост «Стартовал Сезон N»** при первом заходе Survival в новом
  сезоне (`reset()` в [`08-gameplay.ts`](../../../src/game/08-gameplay.ts), ключ
  `toast.seasonStart`). Тост выбран намеренно вместо **чипа на старт-экране** — на момент
  реализации портретная раскладка старт-экрана (см. [`../../backlog.md`](../../backlog.md)) ещё
  не была починена (починена позже, PR #391); тост целится ровно в активных игроков (их же и
  просили — «сначала активным»), не зависит от старт-экрана, а `invite()` уже готов как хук для
  будущего старт-экранного чипа. На
  реальном бэкенде таргетинг/пуш делает сервер — форма `invite()` остаётся хуком UI.
- **Шеринг-карточка** «Я — #X в Серебряном дивизионе, Сезон N»: ✅ **сделано (2026-07-01, вторым
  проходом).** `drawShareCard` в [`10-scene-loop.ts`](../../../src/game/10-scene-loop.ts) рисует
  «💎 Diamond · Сезон 13» под местом в рейтинге (Survival + сезонные данные пришли); нижние
  плитки/график сдвигаются на ту же величину, чтобы не наезжать друг на друга — проверено
  скриншотами (0/1/2 доп. строки над счётом).

### 5. Android: маппинг на нативный Play Games

Play Games даёт **временны́е таблицы (daily/weekly/all-time) из коробки** → «текущий сезон»
можно отразить на weekly/период-таблицу Play Games и получить **реальные кросс-игроковые
позиции бесплатно**, без своего сервера. Дивизионы/промо остаются клиентскими (или на
бэкенде). Проверить точные time-span Play Games при реализации.

---

## Фазы

- ✅ **Фаза 0 — решение (блокер).** Овнер подтвердил сезоны 2026-07-01 (длина 2 недели, 5
  дивизионов, награды — бейджи + косметика, как рекомендовано планом; без мягкой валюты).
- ✅ **Фаза 1 — MVP на mock, без бэкенда — СДЕЛАНА (2026-07-01, двумя проходами), с хвостами.**
  `season`-бакет в `periodBucket` (+тесты) → вкладка «Сезон» на экране рейтинга с отсчётом →
  клиентские дивизионы против сид-ботов → постоянные бейджи дивизиона (мимо «Легенды») → 1
  сезонный косметический приз (акцентный цвет) в отдельном сторе → **вторым проходом:**
  promotion/relegation между сезонами + дивизион в шеринг-карточке. Честная демка — ровно как
  текущий mock-рейтинг. **Хвосты закрыты (2026-07-02, третьим проходом):** подбор ботов под
  уровень игрока (`seasonBotAnchor`), тост-приглашение активным игрокам «Стартовал Сезон N»
  (`season.invite`/`ackInvite` + `toast.seasonStart`) и авто-e2e сезонной вкладки/шеринг-карточки.
  **Осталось:** чип-**отсчёт** на старт-экране (не внешне-заблокирован — портретная раскладка
  самого экрана уже починена, PR #391, см. [`../../backlog.md`](../../backlog.md); просто ещё не
  реализован), мягкая валюта (овнер отклонил — не входила в скоуп) и Фаза 2 (Android
  SDK/Play Console).
- **Фаза 2 — Android-нативка (средняя).** Отразить текущий сезон на период-таблицу Play
  Games (реальные позиции) → приглашение/возврат пушем; шеринг-карточка; завести 5 новых
  дивизион-бейджей в Play Console (дополняет маппинг медалей). Не сделано — блокировано
  отсутствием Android SDK/Play Console в песочнице.
- **Фаза 3 — боевые дивизионы и награды (высокая, блок на бэкенд).** Требует Firebase
  ([`backend-firebase.md`](backend-firebase.md)): сервер считает дивизионы, промо/релегейт,
  выдаёт награды, серверный анти-чит. До него всё соревновательное — демонстрационное. Не
  сделано — веб/Firebase-часть боевого бэкенда SUPERSEDED решением 2026-06-18 (распространение
  только через Google Play), см. [`backend-firebase.md`](backend-firebase.md).

---

## Что НЕ делаем (границы)

- **Реальные Play Points из кейса.** Спонсорская программа Google — партнёрка для крупных
  студий (SYBO), инди туда сейчас не зайти. Награды держим **внутриигровыми** (косметика/бейджи).
- **Сложный матчмейкинг** (возраст, 33 страны, античит-подбор) — не нужен; хватит бракетов по `bestScore`.
- **IAP-first награды** — игра пока без покупок; призы косметические. (Когда появятся IAP —
  сезоны это готовый крючок; см. монетизацию в [`../../backlog.md`](../../backlog.md).)
- **Ломать «медаль = навсегда»** — ротирует только косметика в отдельном сторе.

---

## Анти-чит и честность

Как и у базового рейтинга ([`leaderboards.md`](leaderboards.md#анти-чит-важно-для-боевого-бэкенда)):
клиентский счёт подделывается тривиально, отправка изолирована в `submitRun` (одна точка) —
✅ теперь дополнительно гейтится `ACH.isCleanRun()` (заходы с debug-читами не отправляются, см.
[`leaderboards.md`](leaderboards.md#анти-чит-важно-для-боевого-бэкенда)). На mock сезонные
награды — **демонстрационные**; боевая целостность (реальные дивизионы, призы за деньги/место)
требует серверной валидации из Фазы 3.

---

## Тесты

✅ Расширено [`tests/leaderboard.test.mjs`](../../../tests/leaderboard.test.mjs) (слой 1–2, без
браузера, 13 новых тестов): детерминизм `season`-бакета (границы окна UTC, номер сезона),
`seasonDaysLeft`, назначение дивизиона по перцентилю (`seasonDivisionIndex`),
`Leaderboard.season.standing()`, кумулятивные бейджи дивизиона + `comp:true` + вне «Легенды»
(`ACH.onSeasonDivision`), разведение классов наград (сезонная косметика **не** попадает в
`unlocked`-Set и **не** влияет на «Легенду»), `seasonPromote`/`resolveSeasonDivision` (промо/
релегейт как чистые функции), фиксация дивизиона на весь сезон (повторный вызов `standing()` не
пересчитывает), промо и релегейт на границе сезонов (симулировано прямой записью
`pf_season_division_v1` в localStorage — без манипуляции `Date.now()`, которая тестами не
поддерживается). `validateLeaderboard()` (внутри `validateGame()`) проверяет наличие ключа
`season` в `periodBucket()` и переводов всех 5 бейджей дивизиона. Ручная проверка в реальном
браузере (Playwright) — вкладка «Сезон» (дивизион/отсчёт/приз/промо-индикатор) и шеринг-карточка
(с дивизионом и без) отрисовываются без console-ошибок и без наложения текста.

✅ **Расширено (2026-07-02):** юнит-тесты в [`tests/leaderboard.test.mjs`](../../../tests/leaderboard.test.mjs)
на `season.invite`/`ackInvite` (таргетинг по `bestScore`, показ раз за сезон, сброс на новом
сезоне, порог `minBest`) и на подбор ботов (`seasonBotAnchor`: текущий сезон не масштабирует,
прошлый — масштабирует под уровень; боты не персистятся). ✅ **Авто-e2e добавлен**
([`tests/e2e.spec.mjs`](../../../tests/e2e.spec.mjs), через `window.__GAME`): вкладка «Сезон»
(дивизион + отсчёт, без console-ошибок) и отрисовка шеринг-карточки с сезонными данными.

---

## Открытые вопросы (для овнера) — ОТВЕЧЕНО 2026-07-01

1. ~~**Сезоны — да?**~~ **Да** — подтверждено, реализована Фаза 1.
2. ~~**Длина сезона**~~ — **2 недели**, как у SYBO (рекомендация плана).
3. **Метрика лиги** — Survival `served` (та же метрика, что у базового рейтинга); отдельный
   сезонный скоринг (combo/streak/earned) не введён — не было явного решения предпочесть его.
4. ~~**Награды**~~ — **бейджи + косметика** (без `money`-бонуса), **5 дивизионов** — оба по
   рекомендации плана.
5. ~~**Порядок**~~ — **Фаза 1 сейчас**, боевая — когда будет бэкенд (Фаза 3, отдельно
   заблокирована решением 2026-06-18 про веб/Firebase, см. `backend-firebase.md`).
