# PlaneFlow — что уже сделано

> Архив реализованного, вынесенный из бэклога и копилки идей, чтобы они **не
> мешались** с планами. Здесь — кратко «что и в какой версии»; подробные записи по
> релизам — в [`../CHANGELOG.md`](../CHANGELOG.md), сводка «что в коде сейчас» — в
> [бриф → «Текущее состояние»](tower_project_brief.md). Активные планы и идеи жанра —
> в [`backlog.md`](backlog.md).

## Этапы ядра
- **Этап 1** — ядро диспетчера; **Этап 2.5** — визуал апрона + первичный баланс.
- **Этап 3** — уровни через конфиги `LEVELS`, главное меню, выбор уровней, сохранение
  в `localStorage` (открытые уровни, лучшие звёзды), перепрохождение.
- **Этап 4** — звёзды 0–3 и цели уровней; комбо-множитель и экспресс-бонус;
  динамические события (час пик / смена ветра / туман); Zen-режим (позже заменён на Survival, v0.25).

## Реализованные фишки жанра (по разделам копилки)

### Звук и тактиль
- **Звук на каждое событие** (v0.12) — ноты пентатоники на посадку/бокс/взлёт/
  выделение, синтез Web Audio без файлов; первый шаг к генеративной музыке.
- **Вибрация / haptics** (v0.12) — веб/Android (`navigator.vibrate`); iOS — позже в Capacitor.
- **Нарастающая тревога по терпению** (v0.12) — бип при терпении <25%, учащается к таймауту.
- **Звук необязателен** (v0.12) — тумблеры «Звук»/«Вибрация», всё дублируется визуалом.

### Сочность и иконки (game feel)
- **«Сочность» рисования маршрута** (v0.10–0.12) — бегущий пунктир со свечением,
  пульсирующая точка-цель.
- **Near-miss «уфф»** (v0.14) — мягкая вспышка + лёгкое замедление + whoosh, когда
  два борта едва разошлись (украшение, без штрафа).
- **Плавающие награды и «PERFECT»** (v0.12, v0.14) — «+N ₿» / «−50%» / «−N ₿», а с
  v0.14 ещё «×N комбо» и «вовремя!» всплывают над вылетающим бортом.

### Самолёты: разнообразие
- **Спец-борты как мини-события** (v0.7, v0.16–0.17) — «топливо на нуле» (срочно
  садить, ×1.5), «частный джет» (= вип: ×2 денег, вдвое меньше терпения, золотой
  корпус), «медицинский» (приоритетный, высаживает пациента в пассажирском боксе,
  ×2.5, белый корпус с красным крестом). Срочные борта анонсируются тостом-мини-событием.

### Миссии, цели, прогрессия
- **Звёзды 0–3 с понятными условиями** (Этап 4) + **окно «Цели смены»** в начале
  раунда с разбивкой 1★/2★/3★ (v0.17).
- **Survival — бесконечное выживание на картах** (v0.25; заменил Zen-режим v0.8):
  видимая кнопка «Выживание» в меню (`#survivalBtn` → `showBiomes`) ведёт на карты;
  условие смерти как в кампании (`lives===0`); бесконечный спавн с ростом темпа
  (`survivalPace()` интерполирует `pace` к 1.0 за `SURV_RAMP_SECS`); экран итогов
  показывает счёт бортов; личный рекорд по бортам + счёт в глобальный рейтинг.
- **Лесной биом (🌲) — production-готов** (v0.25+): лесные помехи включены (деревья, олень,
  птицы, снежный занос); бригады: бензопила / техслужба / орёл / снегоуборщик; ледяной бокс
  де-айсинга работает при снегопаде.
- **Арктический биом (❄️) реализован** (v0.26): обледенение ВПП — периодически появляется
  ледяная плёнка, закрывает полосу; тап → деайсинг-грузовик 🚒 едет и расчищает; де-айсинг
  обязателен перед КАЖДЫМ вылетом (погода всегда «снег»); арктический антураж справа от ВПП
  (тундра, сугробы, снежинки); синеватое сервисное здание. Константы ARC (скорость/время работы
  ниже лесных — мороз).
- **Ещё 4 биома Survival реализованы** (PR #253, `3525a01`): 🏝️ Tropical (штормовая волна на ВПП →
  насосная бригада), 🐪 Desert (песчаная буря → пескоочиститель), 🏔️ Mountain (камнепад → бульдозер),
  🌆 Mega City (VIP-кортеж → полицейский эскорт). У каждого свой хазард с бригадой, наградой, тостами
  и декором; конфиги `TROP`/`DSRT`/`MNTN`/`CITY` в `04-config-levels.ts`, логика в `08-gameplay.ts`.
  Все 6 биомов `ready:true` (играбельны из экрана карт). _(Ревизия 2026-07-01: было не в этом файле,
  а FAQ ещё пишет «Скоро» — поправить.)_
- **Инструментарий сложности уровней** (ревизия 2026-07-01): `validatePassable`, `campaignTarget`,
  `autoDifficulty`, `ARCHETYPES`, `archetypeForIndex` в `14-level-analysis.ts` + расширенная модель
  `Objective` (`timeTier`/`maxLate`/`maxCrash`, пер-тир поля, `metric:'survival'`) в
  `04-config-levels.ts`, читается в `computeStars`/валидаторе. В `star-conditions.md` числилось как
  «план» — по факту реализовано.
- **Все production-фичи включены** (v0.26): флаги `DISABLE_VIP/EMERGENCY/MEDICAL/RUSH/WEATHER/
  FOREST/DEICE/SLOWMO/BAY` переведены с `true` на `false` — VIP/аварийные/медицинские борта,
  час пик, погода, помехи на ВПП и де-айсинг теперь работают в игре.
- **Достижения / медали** (v0.10–0.11, +3 ранг-медали с лидербордами) — 49 шт., экран
  «Медали», всплывашки, сохранение, анти-спам выдачи. Подробно —
  в [`achievements.md`](achievements.md).

### Стратегия и глубина
- **Комбо / флоу-множитель** (v0.7) — серия чистых вылетов растит множитель до ×2.
- **Risk/reward** (v0.7) — вип ×2 денег / ×½ терпения; экспресс-бонус ×1.5 за быстрый цикл.
- **Динамические события** (v0.8) — час пик, смена ветра (закрытие ВПП), туман.

### Онбординг и UX
- **Тихий туториал «покажи и дай сделать»** (v0.15) — первый борт ведём за руку.
- **Красивый экран статистики смены** (v0.18) — принято, пик-нагрузка, график во
  времени, доход + кнопка **«поделиться картинкой»** (квадратная карточка → системный
  шеринг или скачивание PNG).
- **Настройки прямо в стартовом меню** (v0.15) — звук, вибрация, язык, сброс прогресса.

### Соц / ретеншн / мета
- **Шеринг результата картинкой** (v0.18) — см. экран статистики выше.
- **«Твоё место» на экране итогов Survival + ранг в шеринге** (2026-06-20) — виджет
  `overRank`/`refreshOverLeaderboard` показывает позицию в срезах all-time/month/week
  сразу на экране итогов захода (тап открывает полную таблицу); ранг попадает и в
  шеринг-карточку.

## Прочее
- **Motion Tuning — полное покрытие параметров** (2026-07-02) — в рантайм-панель `MT_PARAMS`
  вынесены последние зашитые в код параметры движения/визуала: множитель посадочного
  докатывания (`K.LAND_ROLLOUT_MIN`), порог разворота в боксе (`K.BAY_TURN_THRESHOLD`),
  толщина/яркость превью маршрута (`K.ROUTE_WIDTH`/`K.ROUTE_ALPHA`) и длительности анимаций
  HUD/тоста достижения/появления звёзд (`MT.UI_ANIM_DUR`/`ACH_TOAST_DUR`/`STAR_POP_DUR` →
  CSS-переменные `--m-dur`/`--ach-toast-dur`/`--star-pop-dur` через `mtSyncCssVars`). Скорость
  заезда/выезда из бокса и lerp-выравнивание были вынесены ранее (`K.BAY_DOCK_SPEED`,
  `K.LAND_ALIGN_SPEED`, `K.BAY_ALIGN_SPEED`). (motion-tuning.md)
- **Локализация (i18n)** en + ru (v0.9) — таблица строк + `t()`, автоопределение, переключатель.
- **Облик «ночной радар»** по дизайн-прототипу (v0.10).
- **Полноэкранный режим** на телефоне (кнопка + Fullscreen API, защита от прокрутки),
  отступ паузы под safe-area, пустое небо на старте (v0.15).
- **Переименование Tower → PlaneFlow** (v0.13) — название везде + миграция сейва.
- **Реестр биомов `BIOME_DEFS` + `biomeHasHazards()`** (2026-07-02, PR #392) — привязка «биом →
  конфиг/пул помех» сведена из цепочек тернарников (`reset`/`spawnHazard`/`resolveHazard`/
  `biomeCfg`/`dispatchCrew` в `08-gameplay.ts`) в один стол `04-config-levels.ts`; вызов помех
  переведён на `if(biomeHasHazards(LV.biome)) updateForest(dt)` (`08b`). Добавить биом = одна
  строка реестра. Тест-инвариант «каждый ready-биом зарегистрирован». Закрывает пункт бэклога
  «Рефактор при росте биомов (>3)». (biomes.md обновлён)
- **Эмодзи биомов → единый `THEME`** (2026-07-02, PR #392) — `THEME.crew` (бригада → эмодзи) и
  `THEME.emblem` (биом → эмблема сервисного здания) в `01-bootstrap-theme.js`; убран
  дублировавшийся 2× словарь бригад и инлайн-эмблемы из шести biome-draw в `09-render.ts`.
  Первый шаг «единого THEME» (mvp_plan §4.3/§11); вынос цветов/толщин рендера — остаток в
  [`backlog.md`](backlog.md).

## Бренд и визуал
- **Дизайн-система PlaneFlow** интегрирована: спрайты `assets/sprites/neon/`,
  меню на едином языке системы (`menu.css`, токены `--m-*`).
- **Логотип / вордмарк** — `mark` / `wordmark` / `wordmark-lockup`, локап на стартовом экране.
- **Иконка приложения** — 512 full-bleed + Android adaptive (bg/fg) + **maskable** (192/512,
  арт в safe-zone) в `assets/icon/`; подключена как favicon / apple-touch / manifest.
- **Облик neon нарисован** — drawn PNG-атлас (55 спрайтов) в `assets/sprites/neon/` +
  арт меню/бренда; главный экран в этом облике (v0.25).
- **Слим-бренд главного экрана** (2026-06-16–07-01) — кикер и версия убраны из
  `.pf-brand` (остались только вордмарк + тэглайн), тэглайн `Overthinking: OFF. Plane
  traffic: ON.` (en) / «Меньше мыслей — больше рейсов.» (ru), угол `.pf-corner`
  выровнен по `right:7%`. Визуальная доводка процедурной анимации посадки — в
  [`backlog.md`](backlog.md).
- **Портретная раскладка старт-экрана + кнопка рейтинга** (2026-07-01, PR #391) — добавлен
  `@media (orientation:portrait)` для `#startScreen`: вертикальный стек (бренд сверху·по центру ·
  CTA-панель снизу во всю ширину · угловые иконки строкой у низа) вместо ландшафтной раскладки,
  которая перекрывала бренд панелью и резала подписи кнопок. Добавлена кнопка `#leaderboardBtn`
  в `.pf-corner` (обработчик существовал в `13-init.js`, но кнопки в шаблоне не было — тихо
  no-op'ился). Убран неиспользуемый i18n-ключ `start.version`.

## Платформа и публикация
- **PWA** — `manifest.json` + `sw.js`: установка на главный экран, офлайн-старт, тост
  «Доступна новая версия» (навигация network-first → контент обновляется сразу после пуша).
- **Свой домен** `planeflow.jevgenia.com` на GitHub Pages (файл `CNAME`).
- **Android-обёртка (Capacitor)** — упаковка под Google Play: `capacitor.config.ts`,
  нативный мост Play Games (`src/game/12b-native-play-games.ts`), сборка `www/`
  (`npm run build:www`). Раннбук — [`capacitor-android.md`](capacitor-android.md).
  _Легаси TWA-путь (Bubblewrap/`.aab`) — история в_ [`android-twa.md`](android-twa.md).
- **OTA-обновления (self-host, 2026-06-18)** — код игры (`www/`) летит «по воздуху» из **своего
  Contabo VPS** (Caddy авто-TLS + крошечный responder + статика бандлов на `capgo.jevgenia.com`),
  **без облака Capgo и без платы**. CI на push в `main` зипует `www/` и заливает бандл по SSH; плагин
  `@capgo/capacitor-updater`, `updateUrl` в `capacitor.config.ts`, `notifyAppReady()` в `13-init.js`.
  Проверено end-to-end на Pixel 9 (2026-06-18). Детали/грабли — [`capgo-self-host-migration.md`](capgo-self-host-migration.md).
- **Решение: распространение — только Google Play** (2026-06-18) — отдельной поддерживаемой
  веб/PWA-версии как продукта не будет. Следствие: веб-рейтинг на Firebase и веб-фоллбэк **сняты**;
  ачивки/лидерборды — Google Play Games (в коде), облачные сейвы — Play Games Saved Games (в коде,
  PR #149). GitHub Pages остаётся как хост privacy-URL и dev/test-поверхность.
- **Иконки для стора** — 512 + adaptive + maskable (см. «Бренд и визуал»).
- **Developer-аккаунт Google Play верифицирован** (2026-06-15).
- _Осталось до релиза_ (в [`backlog.md`](backlog.md)): скриншоты / feature graphic,
  закрытый трек (нужен release keystore), AdMob.

## Инфраструктура Capacitor + Play Games (завершено 2026-06-17/18)
- **Прокол рисков (Pixel 9, 2026-06-17)** — APK собирается, Canvas/тач в WebView работают,
  ориентация landscape; нативный вход Play Games + экран достижений показывает 5 ачивок, unlock
  зеркалится в PGS. Засады: бридж Capacitor молча не внедрялся без явного `<head>` в HTML
  (исправлено); вход — по жесту игрока (авто-вход Play Games глушит); стек поднят под JDK 21 /
  SDK 36 (AGP 8.7.2 / Gradle 8.9 / compileSdk 36).
- **Play Games в Play Console** — App-запись + PGS-проект (черновик), OAuth consent, тестеры;
  5 ачивок + лидерборд «Survival — Forest» залиты (черновик). ID зафиксированы в
  [`play-games-setup.md`](play-games-setup.md).
- **Мост в коде** (`12b-native-play-games.ts`) — `Account.authProvider`, `Leaderboard.provider`,
  `ACH.unlock` → `unlockAchievement`. Вход по жесту (кнопка «G»). Нативные тосты отложены до
  конца уровня/паузы (гугловскую плашку нельзя приглушить).
- **Облачные сейвы (Play Games Saved Games / Snapshots)** (PR #149, 2026-06-18) — кроссдевайсный
  прогресс. Нативный `SnapshotsPlugin` (шаблон в `setup-android.mjs`, политика
  `RESOLUTION_POLICY_MOST_RECENTLY_MODIFIED`), JS-мост `window.PFCloud` (`12c-cloud-saves.ts`):
  last-writer-wins по `savedAt`, debounce, flush при сворачивании; хук в `saveGame()`. Offline-first:
  `localStorage` — источник правды, облако — зеркало. Архитектура — [`cloud-saves.md`](cloud-saves.md).
  Осталось ручное (в [`backlog.md`](backlog.md)): включить Saved Games в Play Console + тест на устройстве.
- **Privacy / Data Safety** — `privacy.html` (раздел «Google Play Games») и
  [`play-data-safety.md`](play-data-safety.md) готовы; при публикации внести в форму Play Console.

## VPS и OTA-инфраструктура (завершено 2026-06-18)
- **Безопасность VPS** — `PermitRootLogin prohibit-password`, `PasswordAuthentication no`
  (drop-in), UFW: только 22/80/443; fail2ban активен; отдельный пользователь `capgo-deploy`
  (key-only, no-PTY, no-forwarding) для CI-деплоя.
- **Очистка VPS** — снесены delegatron-bot, sandbox-bot, postgresql, docker, banner-qa,
  containerd, пользователь ci-deploy; директории `/opt/delegatron-3000*`, `/opt/banner-qa`,
  `/var/lib/docker`, `/var/lib/postgresql`.
- **Атомарный OTA-деплой** — CI заливает zip и manifest через tmp-файл + SSH `mv` (атомарно);
  retention: хранятся последние 10 бандлов (`ls -t | tail -n +11 | xargs rm -f`).
- **`/health` эндпоинт** — возвращает `{ok, latest_version, bundle_count, disk_free_gb}`;
  HTTP 503 при `ok: false` — UptimeRobot ловит сломанный манифест.
- **Caddy access log** — `/var/log/caddy/capgo.log` (roll 10 МБ, 7 файлов).
- **Google Drive бэкапы** (rclone, scope=drive.file) — бандлы + конфиги ежедневно в 03:00
  (`/opt/backups/backup.sh`); диск >80% → предупреждение в логе.
- **Telegram-алерты** — `capgo-ota.service` OnFailure → Telegram; hourly cron
  `/opt/backups/monitor.sh`: TLS <30 дней, диск >80%, `/health` 503 → Telegram.
- **UptimeRobot** — мониторит `https://capgo.jevgenia.com/health` каждые 5 мин (email при даун).
