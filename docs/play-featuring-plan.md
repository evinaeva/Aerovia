# Google Play «Featuring» — квалификация на редакционные подборки

> Разбор гайда https://play.google.com/console/about/guides/featuring/ и статей, на которые
> он ссылается (`developer.android.com/quality`, `growyouraudience`, `releasewithconfidence`,
> `engage-and-retain-your-users`, гайды по стор-листингу и промо-контенту). Это **отдельная**
> от [`play-level-up-plan.md`](play-level-up-plan.md) программа: Level Up — конкретный
> чек-лист UX/PGS-требований для буста в поиске; Featuring — про попадание в **редакционные
> подборки**, оценивается по 4 «столпам качества» + маркетинг-ассетам. Что уже покрыто
> Level Up-чек-листом — здесь только упомянуто, не повторяется.
>
> **Контекст:** как и Level Up, Featuring даёт эффект только **после** публикации, но часть
> пунктов (маркетинг-копия, ASO, ассеты) дешевле закрыть до релиза, пока карточка ещё не
> отправлена на модерацию.

---

## 1. Core Value

| Требование | Статус в planeflow | Что сделать |
|---|---|---|
| Оригинальность и «полезность/увлекательность» | ✅ есть — механика разбора трафика в потоке, позиционирование через flow-state ([`store-copy.md`](store-copy.md)) | — |
| Соответствие карточки фактическому поведению приложения | 🟡 сверка проведена (2026-07-01), обновлено 2026-07-02 — карточка **точна** в большинстве пунктов (offline ✅, EN+RU ✅, ~49 медалей = «десятки» ✅, 6 биомов `ready:true` ✅); из 2 расхождений 1 закрыто, см. → | (1) ✅ **«10 уровней кампании»** — закрыто (PR #395): клоны L2 в `while(LEVELS.length<50)` заменены настоящими рукописными уровнями L12–L50 (`CAMPAIGN_LAYOUTS`/`CAMPAIGN_PLAN` в [`04-config-levels.ts`](../src/game/04-config-levels.ts)); осталась только балансовая обкатка руками, карточка больше не врёт. (2) **«Survival… попади в глобальный рейтинг»** — лидерборд сейчас mock-данные (экран-трофей), не боевой бэкенд ([`backlog.md`](backlog.md) → «Глобальные рейтинги»); формулировку в [`store-copy.md`](store-copy.md) стоит смягчить или подключить бэкенд до публикации — иначе карточка обещает функцию, которой ещё нет за экраном. **Сложность:** низкая (текст) / зависит от бэкенда (функция). |
| Пиринговый бенчмарк (рейтинг/ретеншен/engagement против похожих игр) | 🚫 недоступно до публикации | После публикации сверяться с Play Console peer benchmarks, не только с абсолютными цифрами. **Сложность:** низкая (после релиза). |

## 2. User Experience

| Требование | Статус | Что сделать |
|---|---|---|
| Большие экраны/foldables, ввод с клавиатуры/геймпада | 🟡 см. Level Up чек-лист (уже отслеживается) | — |
| **Аппаратная кнопка/жест «назад» (Android predictive back)** | 🟡 код готов — [`12f-back-button.ts`](../src/game/12f-back-button.ts) (`@capacitor/app`) закрывает/переключает экраны по приоритету, реюзая существующие `.m-back`-кнопки; вся цепочка навигации прогнана в браузере (клики по тем же кнопкам дают ожидаемые переходы) | Реальный hardware/gesture back на устройстве ещё не прогнан (нужен `npx cap sync android` + сборка) — прогнать вручную из паузы/меню/уровня. **Сложность:** низкая (ручная проверка). |
| **Сохранение состояния жизненного цикла** (Recents, блокировка экрана, релонч) | ⏸️ не проверено | Проверить `pause`/`resume` в Capacitor: возврат из Recents/после блокировки должен восстанавливать именно то, что было, а не сбрасывать в меню. **Сложность:** низкая-средняя. |
| Touch target ≥48dp | ✅ аудировано — реальные интерактивные элементы (`.icon-btn`, `.m-btn`/`.m-btn--icon`) все 48–52px; `.corner-actions`/`.corner-flag`/`.m-btn--sm` (42px/38px) — неиспользуемый CSS, ничего не рендерят | — |
| Контраст текста (4.5:1 / 3:1) | ✅ было ~4:1 (`muted #5f7bb0` на `tarmac-2`/`--m-panel`) — ниже AA; поднято до `#6f88b5` (совпадает с задуманным `--m-text-muted` из дизайн-доки) → ~4.5–5.4:1 | — |
| UX монетизации: без обрыва посреди уровня, размер/частота реклам, диск лоуз до paywall | 🚫 AdMob/IAP не подключены | Заложить как design-constraint **до** интеграции: реклама не мид-левел, никаких сюрприз-биллингов сразу после первого запуска. Относится к пункту «Интеграция AdMob» в [`backlog.md`](backlog.md). **Сложность:** низкая (архитектурное решение, не код сейчас). |
| In-app review API (нативный промпт оценки) | 🟡 код готов — [`12g-in-app-review.ts`](../src/game/12g-in-app-review.ts) зовёт `Capacitor.Plugins.InAppReview.requestReview()` на победе на уровне (после 3-й победы, кулдаун 30 дней сверх годовой квоты Google); нативный мост — `InAppReviewPlugin.java` (Play Core Review API) в [`setup-android.mjs`](../scripts/setup-android.mjs) | Не прогнано на устройстве: `npx cap sync android` + сборка, проверить флоу через Internal App Sharing (промпт показывается всегда в тестовой сборке, независимо от квоты). **Сложность:** низкая (ручная проверка; код готов). |
| Локализация карточки и текстов в игре | 🟡 EN+RU есть, карточка — драфт | См. секцию 5 — конкретика по локализации скриншотов/feature graphic. |
| **ASO: категория + буквальные ключевые слова** | ✅ сделано — категория зафиксирована `Arcade`, буквальные термины («управление воздушным движением», «аэропорт», «ВПП», «авиадиспетчер», «аркада») вписаны в RU-полное-описание, EN-термины таблицей на будущее EN-описание — всё в [`store-copy.md`](store-copy.md) (раздел «Категория и ключевые слова») | — (EN-описание — отдельная задача, но с теми же EN-терминами) |
| **Аспект-рейшио / resize канваса на планшетах, foldables, split-screen** | ⏸️ не проверено — риск специфичен для fixed-playfield canvas-игры (letterbox/растяжение на 4:3, 16:10, динамических пропорциях, resize в multi-window) | Прогнать смену пропорций и resize окна (не только поворот) через [`06-state-layout.ts`](../src/game/06-state-layout.ts) / [`10-scene-loop.ts`](../src/game/10-scene-loop.ts) — конкретнее, чем обзорный пункт «тест на планшете» в Level Up. **Сложность:** средняя. |

## 3. Technical Quality

| Требование | Статус | Что сделать |
|---|---|---|
| 60 FPS / без крашей на референсных устройствах | 🚧 см. Level Up + [`backlog.md`](backlog.md) («Производительность/рендер») | Доп. аргумент: у Google для игр порог — **минимум 30fps на основной цикл, 60fps+ рекомендован для реакционных игр** — planeflow (драг-реакция) целится в 60, это не «желательно», а профильное требование жанра. |
| ADPF, Game Mode API, frame pacing, Android Performance Tuner, Vulkan/ASTC | 🚫 **неприменимо** | Все — нативные GLES/Vulkan-surface инструменты; planeflow рендерится в Canvas2D внутри WebView (та же логика, что уже применена к Vulkan в Level Up doc). Явно зафиксировано, чтобы не пересматривать. |
| Размер сборки / Play Asset Delivery | ✅ измерено — `www/` (веб-часть AAB) ≈ 17 МБ, с большим запасом от лимита базового модуля **150 МБ**; Play Asset Delivery не требуется | — |
| Время старта (launch-to-interactive) | ✅ измерено и исправлен реальный баг — веб-часть боота (до `ATLAS`/спрайтов игра уже интерактивна: рендер процедурный фолбэк) укладывалась в ~250мс, НО `<link rel=stylesheet>` на Google Fonts стоял до игрового `<script>` и блокировал его выполнение до ответа CDN; на недоступной/медленной сети до шрифта это вешало старт на весь сетевой таймаут (замерено ~13с в песочнице с закрытым исходящим трафиком на `fonts.googleapis.com`) — индикатор загрузки не нужен, сама причина устранена | Шрифт переведён на неблокирующую загрузку (`preload`+`onload` свап, [`index.template.html`](../index.template.html)) — игра больше не ждёт шрифт, чтобы стать интерактивной. Регресс закрыт e2e-тестом [`tests/launch-timing.spec.mjs`](../tests/launch-timing.spec.mjs) (держит Fonts CDN недоступным, проверяет бюджет 2с). Нативный старт Capacitor/WebView (до передачи управления веб-части) этим не измерен — вне доступа песочницы без Android SDK/устройства. |
| «Healthy releases» — фиче-килсвитч без релиза в сторе | ✅ код готов — фиче-флаги на Firebase Remote Config: нативный `RemoteConfigPlugin.java` ([`setup-android.mjs`](../scripts/setup-android.mjs), та же Firebase BOM + `firebase-config`) + JS-слой [`12h-remote-config.ts`](../src/game/12h-remote-config.ts) (`Flags.enabled(key)`, дефолты = всё включено, удалённый оверрайд активируется без релиза). Гейты: `survival_leaderboard` → экран рейтинга/кнопка/отправка счёта/ранг-виджет ([`10-scene-loop.ts`](../src/game/10-scene-loop.ts), [`11-menu-ui.ts`](../src/game/11-menu-ui.ts)); `new_achievements` → соревновательные ранг/сезон-медали (`Def.flag` в [`12-achievements-medals.ts`](../src/game/12-achievements-medals.ts)). На вебе плагина нет → дефолты стоят | Осталось на устройстве/консоли: завести ключи `survival_leaderboard`/`new_achievements` (Boolean) в Firebase Remote Config и прогнать выключение на реальной сборке. **Сложность:** средняя (код) / низкая (консоль). |
| Целевой Android API / формат AAB — публикационные требования Play | 🟡 частично — сверено по коду: [`setup-android.mjs`](../scripts/setup-android.mjs) ставит `targetSdkVersion = 36` (Android 16) при `compileSdk 36`, `minSdk 23`; это **выше** текущего минимума Play для новых приложений (API 35 / Android 15 на середину 2026) — с запасом | Значение в коде норм; **минимум Play меняется ежегодно** — пере-сверить `targetSdkVersion` на момент подачи. Формат — AAB (не OBB). **Сложность:** низкая (ручная проверка). |
| Анкета контент-рейтинга (Content rating questionnaire) | 🚫 не заполнена, нигде не отслежена | Добавить как отдельный шаг в чек-лист публикации ([`capacitor-android.md`](capacitor-android.md)) — обязательный пункт Play Console, не связан с кодом. **Сложность:** низкая (ручная). |
| Staged rollout + managed publishing на первом релизе | 🚫 не запланировано | При первой публикации использовать процент постепенной раскатки + managed publishing (контроль точного момента публикации), а не 100% сразу. **Сложность:** низкая (процесс, не код). |
| Firebase Crashlytics + Performance Monitoring | ✅ подключены — gradle-плагины/зависимости в [`setup-android.mjs`](../scripts/setup-android.mjs) (тот же Firebase-проект, что и Analytics), ProGuard keep-правила на line-number-таблицу для читаемых crash-трейсов после R8 | — |

## 4. Privacy & Security

| Требование | Статус | Что сделать |
|---|---|---|
| Data Safety форма | ✅ готова — [`play-data-safety.md`](play-data-safety.md), уже блокер релиза в [`backlog.md`](backlog.md) | — |
| GDPR-консент (EU) | 🚧 код готов, показ временно отключён (см. [`backlog.md`](backlog.md)) | Уже отслежено, отдельного действия здесь не требует. Отдельная от Data Safety обязанность — консент про EU-показ баннера, форма — про декларацию Google. |
| `android:exported` явно на всех компонентах манифеста | ✅ автоматизировано — [`setup-android.mjs`](../scripts/setup-android.mjs) явно ставит `android:exported="true"` на MainActivity (гард от регресса + предусловие App Links) и печатает **аудит-таблицу** всех компонентов манифеста с их `exported` (предпочтительно из мерж-манифеста после сборки, иначе исходного), предупреждая о любом компоненте с `<intent-filter>` без явного `exported` (build-блокер SDK 31+) | Ручная сверка свелась к чтению вывода `setup:android` на реальной сборке (мерж-манифест с компонентами плагинов появляется только после `gradlew`). **Сложность:** низкая. |
| Network security config — запрет cleartext, TLS везде | ✅ добавлено — [`setup-android.mjs`](../scripts/setup-android.mjs) пишет `res/xml/network_security_config.xml` (`cleartextTrafficPermitted="false"`) + ссылку в манифесте; проверено прогоном скрипта на тестовом `android/` | — |
| **Play Integrity API** для анти-чита Survival-лидерборда | 🚫 не реализовано — новый риск-вектор, ранее не упоминался | Конкретный инструмент для уже запланированного пункта backlog «Анти-чит: серверная валидация счёта» — Play Integrity закрывает проверку целостности клиента, до полноценной серверной валидации счёта. **Сложность:** высокая (совпадает с текущей оценкой анти-чита). |
| Vulnerability Disclosure Program (VDP) | 🚫 не заведена | Опционально, низкий приоритет для команды из 2 человек — не блокер. |
| Debug-логи / non-SDK API / debug-библиотеки в релизной сборке | ✅ аудировано — единственный безусловный `console.log` (на каждую потерю борта, [08-gameplay.ts](../src/game/08-gameplay.ts)) удалён; остался только штатный `console.debug`-фолбэк аналитики | — |

## 5. Маркетинговые ассеты и промо (store listing)

> Конкретика ниже уже внесена отдельными правками в [`store-assets-brief.md`](store-assets-brief.md)
> и [`store-copy.md`](store-copy.md) — здесь просто список источника.

- Feature graphic: alt-текст (≤140 симв.), не сплошной белый/чёрный/тёмно-серый фон, цвет
  в тон иконке, без слов «Best/#1/Top/New».
- Скриншоты: alt-текст на каждый, чистый статус-бар (полный заряд/сигнал), подпись ≤20% кадра.
- Скриншоты планшета: **минимум 4** для бейджа «Designed for tablets» (было — «желательно»
  без цифры).
- Промо-видео: отключить рекламу на видео, не возрастное ограничение, встраиваемое (embeddable).
- Локализация текста на скриншотах/feature graphic в RU (сейчас только EN черновик).
- ✅ Короткое описание: `OFF`/`ON`-капс разведён по полям — капс остаётся брендовым хуком
  на feature-graphic, а поле «краткое описание» получило policy-safe вариант без ALL CAPS
  (см. [`store-copy.md`](store-copy.md) → «Краткое описание»).

## 6. Рост и удержание (доп. материал из смежных гайдов)

> Источники: `growyouraudience`, `releasewithconfidence`, `engage-and-retain-your-users`
> (ссылки со страницы Featuring).

- **Пре-регистрация в Play Console** — доступна уже сейчас, до публикации; по кейсам Google
  поднимает day-1 retention/монетизацию. Кандидат в [`backlog.md`](backlog.md) («Потом»).
  **Сложность:** низкая (Play Console, без кода).
- **App Links / deep links** для шеринга результата (счёт/ачивка → ссылка обратно в игру) —
  ✅ код готов: карточка-шеринг ([`10-scene-loop.ts`](../src/game/10-scene-loop.ts)) теперь кладёт
  в `navigator.share` не только картинку, но и текст + deep-link `https://planeflow.jevgenia.com/?screen=survival`;
  роутинг ссылки — [`12i-deep-links.ts`](../src/game/12i-deep-links.ts) (нативный `appUrlOpen` +
  веб-query на загрузке → `showLeaderboard`/`showBiomes`); App Links intent-filter (`autoVerify`)
  в манифесте пишет [`setup-android.mjs`](../scripts/setup-android.mjs); файл верификации —
  [`.well-known/assetlinks.json`](../.well-known/assetlinks.json) (публикуется на Pages в корень
  домена). **Осталось:** вписать в `assetlinks.json` **SHA-256 ключа Play App Signing** (Play
  Console → App Integrity) и прогнать авто-верификацию на устройстве. **Сложность:** средняя (код) /
  низкая (отпечаток + прогон).
- **Live-ops / сезонный контент** — Google называет это одним из ключевых рычагов удержания;
  напрямую подтверждает уже существующий план
  [`design/game-design/season-leagues.md`](design/game-design/season-leagues.md) — не новая
  идея, но независимое подтверждение с источником.
- **Отвечать на отзывы после публикации** — не код, процессная привычка; Google указывает
  среднюю прибавку **+0.7 звезды** у разработчиков, которые отвечают на отзывы.
- **Push-уведомления для ре-энгейджмента** — с явной оговоркой Google не делать их
  спамными/кросс-промо. Не реализовано и не приоритет сейчас, просто зафиксировано на будущее.
- Store listing experiments (A/B иконки/скриншотов/текста, ≥1 неделя на прогон) и custom
  store listings (до 50 шт., таргетинг по стране/источнику) — обе фичи полезны **после**
  старта трафика. Store Listing Events (промо-анонсы контента) — хорошая точка для будущих
  сезонных дропов, тоже пост-запуск.
- Indie Games Accelerator (менторство от Google) — можно рассмотреть после публикации;
  Indie Games Fund — **неприменим** (гранты только для студий из Латинской Америки).
- Genre insights reports пока не покрывают arcade/puzzle (только MMORPG/Battle Royale/4X) —
  просто иметь в виду, действий не требует.

---

## Приоритет (что делать по порядку)

1. **Дешёвые правки до публикации карточки:** ~~ASO-пассы в описании/категории~~ ✅ и
   ~~ALL CAPS сверка~~ ✅ сделаны в [`store-copy.md`](store-copy.md); остаётся — alt-тексты
   и локализация ассетов, минимум 4 скриншота планшета, чек видео
   (реклама/возрастное/embeddable) — всё не код, только тексты/ассеты/Play Console.
2. **Ручные технические проверки перед первым релизом:** ~~touch target/контраст~~ ✅,
   ~~network security config~~ ✅, ~~AAB/150 МБ~~ ✅, ~~debug-логи~~ ✅ — сделаны без устройства
   (код/замер). Остаётся с реальным телефоном: back-кнопка/жест (код готов, не прогнан на
   устройстве), in-app review флоу (код готов, не прогнан — Internal App Sharing), lifecycle
   (Recents/lock/relaunch), `android:exported`, анкета контент-рейтинга.
3. **Архитектурные заделы до интеграции монетизации:** UX-констрейнты рекламы/IAP закладывать
   в дизайн заранее, не постфактум.
4. **Код, не блокирующий релиз:** ~~Crashlytics/Performance Monitoring~~ ✅, ~~in-app review
   API~~ 🟡 (код готов, не прогнан на устройстве) — остаются Remote Config healthy-releases,
   deep links для шеринга.
5. **После публикации:** пре-регистрация (можно и раньше), store listing experiments,
   custom listings, Store Listing Events, Indie Games Accelerator.
6. **Совпадает с уже существующим планом, не новое:** анти-чит (Play Integrity как
   инструмент), Season League (live-ops подтверждение), perf `drawNeonBay` (доп. аргумент
   про 30/60fps порог для игр).

---

## Что НЕ применимо (архитектурная причина)

- **ADPF / Game Mode API / frame pacing / Android Performance Tuner / Vulkan / ASTC** — все
  нативные GLES/Vulkan-инструменты; planeflow — Canvas2D в WebView, тот же вывод, что уже
  сделан для Vulkan в [`play-level-up-plan.md`](play-level-up-plan.md).
- **Indie Games Fund** — гранты только для студий, зарегистрированных в Латинской Америке.
- **Engage SDK / подписочные retention-отчёты** — актуально для мульти-приложений/подписок,
  не для одной инди-игры без подписки.
