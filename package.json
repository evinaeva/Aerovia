{
  "name": "planeflow",
  "version": "0.21.0",
  "private": true,
  "description": "PlaneFlow — airport dispatcher game (single index.html). Tests are a safety net for adding mechanics without breaking old behavior.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "serve": "python3 -m http.server 8123"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
