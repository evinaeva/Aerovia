# OS
.DS_Store
Thumbs.db

# editors
.vscode/
.idea/
*.swp

# node (на будущее — Capacitor / инструменты сборки)
node_modules/
dist/
build/

# Capacitor — генерируемые: webDir (scripts/build-www.mjs) + нативный Android-проект
www/
android/

# СБОРОЧНЫЙ артефакт: index.html собирается из src/ (node scripts/build.mjs),
# его публикует робот при деплое. В git хранится только исходник в src/.
/index.html

# тесты (Playwright)
test-results/
playwright-report/
.playwright/

# python
__pycache__/
*.pyc

# generated sprite outputs (live on the lora-outputs branch, not main)
tools/style-lora/output/

# local preview tooling (Claude Code preview server — not part of the game)
.claude/launch.json
scripts/static-server.mjs
