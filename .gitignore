# OS
.DS_Store
Thumbs.db

# editors
.vscode/
.idea/
*.swp

# node (на будущее — Capacitor / инструменты сборки)
node_modules/
dist/
build/

# Capacitor — генерируемые: webDir (scripts/build-www.mjs) + нативный Android-проект
www/
android/

# Release signing (upload-key) — машинно-локальные СЕКРЕТЫ, НИКОГДА не коммитить.
# Формат/инструкция — keystore.properties.example + docs/capacitor-android.md.
keystore.properties
*.keystore
*.jks

# СБОРОЧНЫЙ артефакт: index.html собирается из src/ (node scripts/build.mjs),
# его публикует робот при деплое. В git хранится только исходник в src/.
/index.html

# СБОРОЧНЫЙ артефакт: tuning.html (dev-воркбенч) собирается из src/tuning/ +
# src/tuning.css (node scripts/build-tuning.mjs). Робот пересобирает его при
# деплое; в git — только исходники в src/.
/tuning.html

# тесты (Playwright)
test-results/
playwright-report/
.playwright/

# python
__pycache__/
*.pyc

# generated sprite outputs (live on the lora-outputs branch, not main)
tools/style-lora/output/

# path-sim render outputs (regenerate with `node tools/path-sim.mjs`)
tools/_pathsim/

# local preview tooling (Claude Code preview server — not part of the game)
.claude/launch.json
scripts/static-server.mjs

# local design explorations & presentation (kept on disk, not tracked)
.tmp_design/
scripts/build-presentation.mjs
PlaneFlow — история версий.pdf
